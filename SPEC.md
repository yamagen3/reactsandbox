# React Sandbox - 仕様書 (Spec-Driven)

## 1. プロジェクト概要

### 1.1 目的
Reactを静的環境で練習できるブラウザベースのSandboxアプリを提供する。

### 1.2 目標ユーザー
- React初心者
- Reactの機能を試したい開発者
- オフライン環境でReactを学習したい人

### 1.3 デプロイ環境
GitHub Pagesを使用した静的サイトとして配信

## 2. 機能要件

### 2.1 コアフィーチャー

#### F1: コードエディタ
**優先度**: 高
**説明**: ユーザーがReactのJSXコードを入力・編集できるテキストエディタ

**要件**:
- テキストエリアでのコード入力
- Tabキーによるインデント機能（2スペース）
- リアルタイムでの変更反映
- 初期値としてサンプルコードを表示

**テスト条件**:
- ユーザーがコードを入力できる
- Tabキーで2スペースが挿入される
- コードの変更が親コンポーネントに伝わる

#### F2: ライブプレビュー
**優先度**: 高
**説明**: エディタで入力されたコードをリアルタイムで実行し、結果を表示

**要件**:
- iframeを使用した安全なコード実行環境
- Babelによる自動JSXトランスパイル
- React 18のサポート
- React Hooksのサポート（useState, useEffect, useRef等）
- エラー時の適切なエラーハンドリング

**テスト条件**:
- 正しいJSXコードが実行される
- エラーがあるコードでもアプリがクラッシュしない
- React Hooksが正常に動作する

#### F3: エラー表示
**優先度**: 高
**説明**: コードにエラーがある場合、ユーザーに分かりやすくエラーを表示

**要件**:
- 構文エラーの表示
- ランタイムエラーの表示
- エラーメッセージの視覚的な区別

**テスト条件**:
- 構文エラーが発生した際にエラーメッセージが表示される
- エラーが修正されるとエラーメッセージが消える

#### F4: リセット機能
**優先度**: 中
**説明**: コードをデフォルトのサンプルコードに戻す

**要件**:
- ワンクリックでリセット
- デフォルトコードは動作する完全な例

**テスト条件**:
- リセットボタンをクリックするとデフォルトコードに戻る

### 2.2 UI/UX要件

#### U1: レスポンシブレイアウト
- エディタとプレビューを横並びで表示
- 画面サイズに応じた適切な表示

#### U2: 日本語対応
- すべてのUIテキストは日本語
- エラーメッセージも可能な限り日本語

#### U3: 読み込み状態の表示
- コード実行中はローディングインジケーターを表示

## 3. 技術仕様

### 3.1 技術スタック
- **React 18**: UIライブラリ
- **Vite**: ビルドツール
- **Babel Standalone**: ブラウザ内JSXトランスパイル
- **Vitest**: テストフレームワーク（TDD）
- **React Testing Library**: コンポーネントテスト

### 3.2 コンポーネント構成

```
App
└── Sandbox
    ├── CodeEditor (エディタ機能)
    └── Preview (プレビュー機能)
```

#### 3.2.1 App
- ルートコンポーネント
- ヘッダーとSandboxを表示

#### 3.2.2 Sandbox
- 状態管理（コード、エラー）
- デフォルトコードの管理
- リセット機能の提供

#### 3.2.3 CodeEditor
- Props: `code`, `onChange`
- Tabキーのハンドリング
- テキストエリアの管理

#### 3.2.4 Preview
- Props: `code`, `onError`
- iframeによるコード実行
- Babelトランスパイル
- エラーハンドリング

### 3.3 データフロー

1. ユーザーがCodeEditorでコードを入力
2. `onChange`でSandboxの状態を更新
3. Previewコンポーネントが新しいコードを受け取る
4. BabelでJSXをトランスパイル
5. iframe内でコードを実行
6. エラーがあればSandboxに通知
7. エラーメッセージを表示

## 4. 非機能要件

### 4.1 パフォーマンス
- 初回ロード時間: 3秒以内
- コード実行のレスポンス: 500ms以内

### 4.2 セキュリティ
- iframeのsandbox属性を使用
- XSS攻撃の防止

### 4.3 ブラウザサポート
- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

## 5. GitHub Pages デプロイ

### 5.1 ビルド設定
- Base URL: リポジトリ名に対応
- 静的アセットの最適化

### 5.2 GitHub Actions
- main/masterブランチへのpush時に自動デプロイ

## 6. テスト戦略（TDD）

### 6.1 単体テスト
- 各コンポーネントの独立したテスト
- カスタムフックのテスト

### 6.2 統合テスト
- コンポーネント間の連携テスト
- ユーザーフローのテスト

### 6.3 テストケース
1. コードエディタでのテキスト入力
2. Tabキーによるインデント
3. コードの実行とプレビュー
4. エラー時のエラーメッセージ表示
5. リセット機能

## 7. 今後の拡張可能性

- コードのローカルストレージ保存
- 複数のコードサンプル
- ダークモード
- コードのエクスポート/インポート
- シンタックスハイライト
